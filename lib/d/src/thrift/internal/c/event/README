This package contains a direct translation of those parts
of the libevent headers needed for the async client and
non-blocking server implementations.

They currently live in the thrift.internal package as to
not collide with any more comprehensive, already installed
modules.
